function sum(...numbers) {
    let hasil = 0;
    for(const number of numbers) {
        hasil = hasil + number;
        15
    }
    console.log(hasil);
}
sum(10, 5, 9, 6, 8, 7);

// rest operator untuk menggabungkan